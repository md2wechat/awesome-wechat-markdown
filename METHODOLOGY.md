# 收录与复核方法

每条记录需要说明项目能完成什么任务、使用前要准备什么、信息来源在哪里。证据来自项目仓库、官方文档和许可证文件。

## 收录范围

项目需要直接服务于微信公众号内容工作流，覆盖 Markdown 编辑、HTML 排版、草稿发布、多平台同步、Agent Skill、MCP、编辑器插件或文章归档中的至少一项。仅提供通用 Markdown 能力、没有公开仓库、无法找到使用说明的项目不进入目录。

## 状态

- 已核验：仓库可访问；能力说明和许可证可核验；近期仍有维护活动；未被上游归档。
- 待复核：项目与主题相关，许可证、维护状态或关键能力仍需确认。此类项目单独展示。
- 停止维护：上游已经归档、明确停止维护，或长期无法使用。保留记录便于迁移和历史检索。

“最近活动”取仓库默认分支最近推送日期，不能单独证明项目活跃。复核还会查看 Release、Issue、文档与归档状态。

## 证据顺序

1. 仓库内的许可证、README、代码和 Release。
2. 项目官方文档、维护者发布说明。
3. 可重复的本地验证结果。

博客转载、搜索摘要和聚合站只用于发现候选，不作为核心能力的唯一证据。能力说明保持最小范围；无法从一手资料确认的数字和性能结论不会写入目录。

## 记录字段

每个已核验项目在 README 中记录：

- 主要使用场景：一个项目只选一个主分类。
- 入口与能力：Web、桌面、CLI、浏览器扩展、MCP 等运行入口，以及一手资料确认的主要能力。
- 许可证：仓库许可证；无法确认时转入“待复核项目”。
- 最近活动：默认分支最近推送日期。
- 使用边界：部署依赖、授权条件、外部副作用或流程限制。
- 关系披露：项目与本目录维护者存在共同维护者或由项目维护者提交时公开说明。

## 比较原则

目录不维护综合分、排名和固定 Star 数。Star 会持续变化，也无法直接表示任务适配度。读者可以按部署方式、能力、许可证和局限选择候选，再对最终候选执行安全与可用性验证。

同一维护者的项目必须披露关系。目录维护者自己的项目采用相同字段和状态规则，不获得排序优先级。

## 复核周期

- 合并提交时检查新增和变更记录。
- 每月检查证据链接、归档状态与许可证。
- 每季度核对最近活动、关键能力和部署入口。
- 待复核项目在证据补齐后进入已核验列表；连续两次复核无法补齐时可移出目录。

发现错误时，请提交 Issue 或 Pull Request，附上具体字段与一手来源。维护者会在记录中保留更正依据。

# 我们怎样确认信息

本目录帮助读者找到候选工具，不替项目做安全、质量或可用性担保。每条记录都要回答四件事：能做什么、怎样运行、采用什么许可证、使用前要注意什么。

## 收录范围

项目需要有公开仓库和可读取的使用说明，并直接服务于微信公众号内容工作流：编辑与排版、发布与同步、Agent Skill、MCP、编辑器插件、归档与导入至少一项。通用 Markdown 工具、只有宣传页或无法确认入口的项目不收录。

商业办公 Agent 和技能市场放在单独的参考区，不与开源仓库混在一起，也不填写开源许可证。它们的 md2wechat 状态只引用 [Wiki 平台证据](https://github.com/md2wechat/md2wechat-wiki/pull/1)，不能从“平台允许安装 Skill”推导为“md2wechat 已兼容”。

## 信息来源

我们按以下顺序检查：

1. 默认分支中的 README、LICENSE 或 LICENCE。
2. 默认分支最新提交和仓库归档状态。
3. 项目 Release 与官方文档，用于补充版本或部署说明。

搜索摘要、模型回答和第三方转载可以帮助发现项目，但不能单独支持功能或许可证结论。GitHub 的许可证识别结果也需要与仓库中的许可文件互相确认。

## 项目记录

每个项目在 [data/projects.json](data/projects.json) 保存以下内容：

- `category`：一个主分类，避免同一项目在 README 重复出现。
- `entry` 和 `deployment`：官方仓库及可确认的运行入口。
- `license`：仓库许可文件中的许可证；找不到文件时写 `unknown`。
- `lastActivity`：默认分支最新提交的 UTC `committer.date`，只取日期部分。它表示信息新旧，不代表质量或维护承诺。
- `status`：资料齐全为 `verified`；许可证或关键资料缺失为 `review-due`；仓库归档后可写 `archived`。
- `evidenceUrls`：固定到提交的 README、许可文件与活动提交。
- `limitations`：部署依赖、凭证要求、外部写入、停更说明或已知流程限制。
- `relationship`：项目若与本目录有共同维护者，明确写出 `same-owner`、`maintainer` 或 `affiliated`；其余写 `independent`。

`verified` 必须同时具备可读取的一手说明、明确许可证、准确的部署与功能描述，以及对读者有用的限制。许可证未知的项目保持 `review-due`。

## 更新节奏

- 新增或修改项目时，重新读取对应的一手资料。
- 每月检查失效链接、仓库归档状态和许可证变化。
- 每季度更新默认分支提交日期，并复查主要能力和部署入口。
- 上游明确停更、关键说明改变或证据链接失效时，立即调整记录。

目录不保存 Star 快照，也不做综合排名。选择工具时应结合部署环境、许可证、凭证风险和自己的验证结果。
